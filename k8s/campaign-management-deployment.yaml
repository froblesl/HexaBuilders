apiVersion: apps/v1
kind: Deployment
metadata:
  name: campaign-management
  namespace: hexabuilders
  labels:
    app: campaign-management
    component: application
spec:
  replicas: 1  # Reducido para minimizar costos
  selector:
    matchLabels:
      app: campaign-management
  template:
    metadata:
      labels:
        app: campaign-management
        component: application
    spec:
      containers:
      - name: campaign-management
        image: gcr.io/PROJECT_ID/hexabuilders-campaign-management:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 5003
          name: http
        env:
        - name: PYTHONPATH
          value: "/app/src"
        - name: DATABASE_URL
          value: "postgresql://hexabuilders_user:hexabuilders_password@postgres:5432/hexabuilders"
        - name: PULSAR_BROKER_URL
          value: "pulsar://broker:6650"
        - name: PULSAR_ADDRESS
          value: "broker"
        - name: PULSAR_ADMIN_URL
          value: "http://broker:8080"
        - name: FLASK_ENV
          value: "production"
        - name: SERVICE_PORT
          value: "5003"
        livenessProbe:
          httpGet:
            path: /health
            port: 5003
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 5003
          initialDelaySeconds: 15
          periodSeconds: 5
        resources:
          requests:
            memory: "128Mi"  # Reducido para minimizar costos
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: campaign-management
  namespace: hexabuilders
  labels:
    app: campaign-management
    component: application
spec:
  type: ClusterIP
  ports:
  - port: 5003
    targetPort: 5003
    name: http
  selector:
    app: campaign-management
